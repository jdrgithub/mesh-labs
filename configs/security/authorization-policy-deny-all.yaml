# Deny-all authorization policy for security-first approach
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: deny-all
  namespace: bookinfo
spec:
  # Empty rules means deny all
  rules: []